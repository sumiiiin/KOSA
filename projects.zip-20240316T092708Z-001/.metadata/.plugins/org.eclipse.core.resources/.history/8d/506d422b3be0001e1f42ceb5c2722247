package ch08.sec11.exam02;

public class DriverExample {

	public static void main(String[] args) {
		
		Driver driver = new Driver();
		
		driver.drive(new Bus()); // Vehicle로 사용가능한 객체 사용 가능
		
		Vehicle vehicle = new Texi();
		driver.drive(vehicle); // Vehicle로 사용가능한 객체도 사용 가능 
		

	}

}
